function SaveElection() {

    // Add the election to the json db

    const onAdd = () => {

    }
    return (
        <button type="button" onClick={onAdd}>
            SaveElection
        </button>
    )
};

export default SaveElection;